@model IEnumerable<BTL.Models.Category>

@foreach (var item in Model)
{
    <li>
        <a href="@Url.Action("showMenShirtsByIDCategory","MenShirts", new {id = item.CategoryID})">
            @item.CategoryName
        </a>
    </li>
}
